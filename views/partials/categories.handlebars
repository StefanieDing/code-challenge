{{#each category}}
  <div class = "col s4" id={{this.id}}>
    <i class="fa fa-{{this.icon}}"></i> 
    <h4 class="cat-title"><b>{{this.title}}</b></h4>
    <h6 class="cat-description">{{this.description}}</h6>
  </div>
{{/each}}
